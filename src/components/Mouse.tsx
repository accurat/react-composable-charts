// import { Mouse, useMouseContext } from './internal'

// export interface MouseProps {
//   children?: (mouse: Mouse | undefined) => React.ReactNode
// }

// export const MouseConsumer = ({ children }: MouseProps) => {
//   const mouse = useMouseContext()
//   return children ? children(mouse) : null
// }
